{
  "name": "hattrick-extended",
  "version": "0.1.0",
  "description": "Various enhancements for Hattrick",
  "license": "GPL-3.0",
  "type": "module",
  "main": "index.js",
  "scripts": {
    "build": "esbuild src/index.ts --bundle --outfile=dist/bundle.js --target=firefox115,chrome120",
    "watch": "esbuild src/index.ts --bundle --outfile=dist/bundle.js --target=firefox115,chrome120 --watch",
    "clean": "rm -rf dist/"
  },
  "packageManager": "yarn@4.12.0",
  "devDependencies": {
    "esbuild": "^0.27.2",
    "typescript": "^5.9.3"
  }
}
